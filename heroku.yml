build:
  languages:
    nodejs: true
  packages:
    - node-12.16.1
  config_vars:
    NPM_CONFIG_PRODUCTION: false
  scripts:
    heroku-prebuild: "npm --prefix server install && npm --prefix client install"
    heroku-postbuild: "npm --prefix client run build"
run:
  server: npm start --prefix server
